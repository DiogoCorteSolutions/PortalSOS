@model IEnumerable<PortalSOS.Dommain.Produto_Dommain>
@{
    ViewBag.Title = "Lista de Produto";
}
<h2>@ViewBag.Title</h2>


<blockquote>
    <p>@ViewBag.Title</p>
    <small>Bem <cite title="Source Title">Vindo</cite></small>
</blockquote>

@*<blockquote>
    <p>@ViewBag.Title</p>
    <small>Bem <cite title="Source Title">Vindo!!!</cite></small>
</blockquote>*@

@*@if (TempData["msgerror"] != null)
{
    <div class="alert alert-dismissible alert-danger">
        <button type="button" class="close" data-dismiss="alert"></button>
    @TempData["msgerror"]
    </div>
}*@

<hr />

@if (TempData["msgerror"] != null)
{
    <div class="alert alert-dismissible alert-danger">
        <button type="button" class="close" data-dismiss="alert"></button>
        @TempData["msgerror"]
    </div>
}
else if (TempData["msgsucesso"] != null)
{
    <div class="alert alert-dismissible alert-sucess">
        <button type="button" class="close" data-dismiss="alert"></button>
        @TempData["msgsucesso"]
    </div>
    
}


@*else if (TempData["msgsuccesso"] != null)
{
    <div class="alert alert-dismissible alert-success">
        <button type="button" class="close" data-dismiss="alert"></button>
        @TempData["msgsucesso"]
    </div>
}*@

@*@using (Html.BeginForm("listarParticipante", "cliente", FormMethod.Get, new { @class = "form-horizontal", id = "formEdit", name = "formEdit" }))
{
    <div class="form-group">
        <label class="col-lg-2 control-label">CPF</label>
        <div class="col-lg-4">
            <input type="text" class="form-control" name="cpf" id="cpf" />
        </div>
        <div class="col-lg-4">
            <button type="submit" class="btn btn-default">Pesquisar</button>
        </div>
    </div>

    <div id="alerta-div" style="display: none;"></div>
}*@


<table id="example" class="display" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th style="text-align: center;">Id</th>
            <th style="text-align: center;">Nome</th>
            @*            <th style="text-align: center;">CPF</th>
            <th style="text-align: center;">CNPJ</th>
            <th style="text-align: center;">Perfil</th>
            <th style="text-align: center;">Razão Social</th>*@
            <th style="text-align: center;">Status</th>
            @*            <th style="text-align: center;">Ações</th>*@
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr style="text-align: center;">
                <td>@item.IdProduto</td>
                <td>@item.Linha</td>
                <td>@*@((item.AdminParticipanteLojas.Count() > 0 && item.AdminParticipanteLojas.First().Loja != null) ? item.AdminParticipanteLojas.First().Loja.CNPJ : "")</td>
                <td>@((item.ParticipantePerfils.Count() > 0 && item.ParticipantePerfils.First().Perfil != null) ? item.ParticipantePerfils.First().Perfil.Nome : "")</td>
                <td*@>@*@((item.AdminParticipanteLojas.Count() > 0 && item.AdminParticipanteLojas.First().Loja != null) ? item.AdminParticipanteLojas.First().Loja.RazaoSocial : "")*@</td>
                <td>@item.Stat</td>
                @*     <td>
                    <a href="@Url.Action("AlterarPerfil", "Cliente", new { id = item.Id })" data-toggle="tooltip" title="Editar">
                        <img width="20" src="~/Content/img/edit.png" /></a>
                    <a href="@Url.Action("visualizar", "cliente", new { id = item.Id })" data-toggle="tooltip" title="Visualizar">
                        <img width="20" src="~/Content/img/view.png" /></a>
                </td>*@

            </tr>
        }
    </tbody>
</table>

<div class="form-group">
    <div class="col-lg-8 col-lg-offset-2">
        <form method="get" action="@Url.Action("listarProduto", "produto")">
            <button type="submit" class="btn btn-success">Buscar</button>
            <a href="@Url.Action("listarParoduto", "produto")" class="btn btn-danger">Buscar</a>
        </form>
    </div>
</div>


